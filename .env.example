# Optional: override the GA4 Measurement ID baked into app/layout.tsx.
# Useful if you want a separate analytics stream for preview deploys.
# Get the ID from https://analytics.google.com -> Admin -> Data Streams.
NEXT_PUBLIC_GA_ID=
